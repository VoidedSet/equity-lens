# News Pipeline

Daily hospitality news pipeline aligned to PRD checks. Produces digests and graph events.
Install dependencies and run:

```bash
pip install -r requirements.txt
python news_pipeline.py
```
